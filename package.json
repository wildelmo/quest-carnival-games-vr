{
  "name": "quest-carnival-games-vr",
  "version": "0.1.0",
  "description": "WebXR carnival arcade for Meta Quest — ball toss, balloon darts and more, inside a funhouse circus tent",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
